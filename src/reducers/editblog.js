const initialState = {
    
}

const editblogReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default editblogReducer;