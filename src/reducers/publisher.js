const initialState = {
    
}

const publisherReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default publisherReducer;